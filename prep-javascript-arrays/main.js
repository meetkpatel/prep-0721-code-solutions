var languages = ['html',
  'css',
  'javascript',
  'react',
  'node',
  'postgres'];

console.log(languages);

var firstElement = languages[0];
console.log(`The first element of array is: ${firstElement}`);

var thirdElement = languages[2];
console.log(`The 3rd element of array is: ${thirdElement}`);

var length = languages.length;
console.log(`The length of array is: ${length}`);

var lastElement = languages[length - 1];
console.log(`The last element of array is: ${lastElement}`);
